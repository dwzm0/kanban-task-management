/* import {array, object} from "zod";

export const createDashboardSchema = object({
    body: object({
        name: string({
            require_error: 'Please provide name for new dashboard.'
        }),
        columns: array() 
    })
});  */